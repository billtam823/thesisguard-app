# AGENTS.md

This is a Vite + React 18 web app (MUI, TanStack Query). It is **not** an Expo project — ignore the root-level `App.tsx`/`app.json`/`index.ts` leftovers; the entry point is `index.html` → `src/main.tsx`.

See `CLAUDE.md` for full project documentation, and `../thesisguard-api/README.md` for the overall system.
