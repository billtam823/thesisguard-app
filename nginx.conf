server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: serve index.html for client-side (react-router) routes,
    # so a refresh on e.g. /stocks/NVDA doesn't 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
